import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DeepNeurax - AI Solutions Provider",
  description: "Transform your business with cutting-edge AI solutions. We deliver intelligent automation, machine learning, and data-driven insights.",
  keywords: ["AI", "artificial intelligence", "machine learning", "automation", "deep learning"],
  openGraph: {
    title: "DeepNeurax - AI Solutions Provider",
    description: "Transform your business with cutting-edge AI solutions.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
